# use these commands to create the workspace

npm create vite@latest pg-ray -- --template react
cd pg-ray
npm install reactflow lucide-react clsx tailwind-merge
npm install -D tailwindcss postcss autoprefixer